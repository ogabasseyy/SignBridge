package com.signbridge.navigation

enum class AppDestination(val route: String) {
    Home("home"),
    SignToSpeech("sign-to-speech"),
    Listen("listen"),
    Emergency("emergency"),
    Settings("settings"),
}
